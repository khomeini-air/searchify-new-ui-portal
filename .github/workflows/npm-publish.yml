name: NodeJS with Webpack

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v3
#     - name: Build & Run
#       run: |
#         sudo npm install
#         sudo npm run build
#         sudo npm run start
